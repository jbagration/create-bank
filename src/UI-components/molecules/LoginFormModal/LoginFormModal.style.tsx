import { Theme } from '@mui/material'

export const loginFormTypographyStyles = (theme: Theme) => {
    return {
        marginBottom: '24px',
        textAlign: 'center',
        color: theme.palette.Text.TextPrimary,
    }
}
